Here is your complete polishing prompt:

---

You are a senior React/TypeScript engineer and UI polish specialist. The Trade School Simulator app already exists with its full structure — `useTradingEngine.ts`, `chartEngine.ts`, `phases.ts`, `CandlestickChart.tsx`, and `Index.tsx`. The UI is built. The logic exists. Your job is NOT to rebuild from scratch. Your job is to find every broken, incomplete, janky, or disconnected thing and fix it, polish it, and make it feel like a finished, premium, world-class product. Go through every file systematically and apply every improvement below.

---

## 1. FIX THE CORE LOOP FIRST

The app currently has no clear "what do I do next" flow. A user lands on the screen and has no idea what is expected of them. Fix this:

- On first load, ARIA must automatically appear after a 1.5 second delay with an animated entrance and deliver the Phase 1 briefing via typewriter animation. This is not optional — it must happen every time the user is on a phase they haven't completed.
- Each phase must have a single, clearly visible **active challenge card** displayed prominently above the trade controls. This card shows: the challenge title, a one-line instruction, the specific criteria (e.g. "Enter a BUY trade with a stop loss set below the last swing low"), and an XP reward badge. This card cannot be dismissed until the challenge is completed or failed.
- When the challenge criteria are met by a closed trade, trigger the success sequence immediately: ARIA celebrates, XP animates into the bar, the card flips to a green "Complete" state, and after 3 seconds the next challenge card slides in. If the user closes a trade without meeting criteria, ARIA explains specifically what was missing.
- Phase advancement must feel like an event. When a phase is completed, play a full-screen overlay cinematic: the current phase node on the skill tree glows and pulses, a beam of light connects it to the next node, the new phase title types itself onto the screen, and ARIA delivers the next phase briefing. This overlay auto-dismisses after 5 seconds or on click.

---

## 2. ARIA — MAKE HER REAL

She currently exists as text. Make her feel like an actual intelligent presence:

- She must have three visual states with smooth CSS transitions between them: **Teal** (teaching/neutral), **Amber** (caution — no SL set, bad R:R, over-leveraged), **Red** (danger — revenge trading detected, 3 losses in 10 minutes, user moving SL against themselves).
- Her avatar border must pulse with a slow breathing animation at all times (CSS keyframe, 3s cycle, opacity 0.4 to 1.0 on the border glow).
- Every message she sends must use a typewriter effect. Characters appear one by one. Do not dump the full string instantly.
- She must speak during an open trade every 10–15 seconds with commentary that is genuinely specific to what is happening on the chart: trend direction, whether price is approaching S/R, whether the trade is in profit, whether the setup is playing out as expected. This is not generic filler — pull actual chart state into the message.
- Revenge trading detection: track timestamps of last 5 closed trades. If 3 or more are losses and the time between them is under 4 minutes each, trigger ARIA red mode and disable the submit button with a 45-second cooldown. Show a visible countdown timer on the button.
- ARIA's message history must be scrollable. Show the last 8 messages in a chat-log style inside her panel with timestamps. Older messages fade to lower opacity.

---

## 3. CHART — MAKE IT FEEL ALIVE

- Every new candle must animate. The wick extends first over 150ms (easeOut), then the body fills from open toward close over 200ms (easeInOut). Use requestAnimationFrame, never setTimeout for this.
- The current price line must be a dashed horizontal line that follows price with a small label box on the right axis. The label background must alternate color based on whether current price is above or below the previous close (teal for up, red for down). The label opacity must breathe (CSS animation, subtle).
- Implement smooth pan and zoom: mouse wheel zooms in/out centered on cursor position, click-drag pans left/right. Zoom range: 20 candles minimum to 200 candles maximum visible at once. Current zoom/pan state must persist across re-renders using a ref.
- When ARIA is in Teaching mode at the start of a phase, she draws annotations directly on the chart canvas: swing high/low arrows (small triangles above/below candles), trend lines in amber, and a glowing rectangle border around the specific candle pattern she is teaching. These annotations fade out after 8 seconds.
- Support/Resistance zones must render as semi-transparent horizontal bands (not single lines): support in rgba(29,158,117,0.1) with a solid 1.5px teal left-edge line, resistance in rgba(226,76,74,0.1) with a solid 1.5px red left-edge line. Each zone has a small label on the right showing "S" or "R" and the price level.
- Crosshair on hover: thin dashed white lines, OHLCV tooltip in a dark glass card at the top-left of the chart. Price snaps to nearest candle on the x-axis.

---

## 4. GAMIFICATION — MAKE IT MATTER

- The XP bar in the top bar must animate every time XP is earned: the fill smoothly extends over 600ms with a brief shimmer sweep animation passing across it. If leveling up, the bar fills to 100%, flashes white, then resets and continues filling with the overflow XP.
- Achievements must have a popup notification system. When an achievement unlocks, a card slides in from the top-right corner, shows the achievement icon, name, and description for 4 seconds, then slides back out. Never show more than 2 stacked at once — queue them.
- The Skill Tree modal must be a genuine visual tree, not a list. Render it as an SVG or canvas with nodes connected by lines. Completed nodes are solid teal circles with a checkmark. Current node pulses. Locked nodes are dark with a lock icon. Lines between nodes light up as phases complete.
- Challenges in the left panel must show a live progress bar. If the challenge is "Execute 3 trades with a stop loss," the bar shows 1/3, 2/3, 3/3 filling in real time as qualifying trades are closed.
- Session end screen: when the user manually ends a session or after 20 trades, show a full-screen results overlay. Display: total trades, win rate, best trade, worst trade, discipline score (0–100 based on SL usage, R:R adherence, no revenge trading), ARIA's written verdict on the session, and XP earned. Include a "Trade Again" and "Review Journal" button.

---

## 5. TRADE PANEL — MAKE IT FUNCTIONAL AND BEAUTIFUL

- Lot size input must be a slider AND a number input synchronized. Moving the slider updates the number field instantly and vice versa. Below it, show the calculated risk in dollars and as a percentage of balance in real time as the slider moves, colored green if under 1%, amber if 1–2%, red if above 2%.
- Stop Loss and Take Profit inputs must auto-suggest values. When a user sets a SL, the TP field should auto-populate with a value that gives exactly 2:1 R:R. User can override this. Both fields show the pip distance from current price in small muted text below them.
- The Buy button must be teal, the Sell button red-orange. Both must have a subtle glow on hover. When the market is not in a tradable state (ARIA is mid-lesson, cooldown active), both buttons are disabled with a clear visual state and tooltip explaining why.
- An open trade must show a live P&L display updating every tick: current price, entry price, distance in pips, unrealized P&L in dollars, color coded green/red. Include a "Close Trade" button that is red and confirms before closing.
- Risk/Reward ratio must display as a visual ratio bar: two colored segments showing the SL distance vs TP distance. Updates live as user adjusts SL and TP.

---

## 6. PRICE ENGINE — MAKE IT BELIEVABLE

In `chartEngine.ts`, fix and improve the price simulation:

- Market regimes must transition smoothly. When switching from Uptrend to Range, do not jump abruptly. Insert a 10–15 candle "transition" regime where volatility increases and direction becomes ambiguous before settling into the new regime.
- Implement proper higher highs / higher lows structure in uptrends and lower highs / lower lows in downtrends so the trend is visually obvious and ARIA can correctly identify and annotate swing points.
- Add realistic volatility clustering: periods of low volatility (small candle bodies, tight wicks) followed by expansion (large candle bodies). This should feel organic, not mechanical.
- S/R levels must be calculated from actual price history: find pivot highs (candle where high is the highest of surrounding 5 candles) and pivot lows (candle where low is the lowest of surrounding 5 candles). Group levels within 8 pips of each other into a zone. Show the top 3 resistance and top 3 support zones at any given time. Recalculate every 20 candles.
- Breakout regime: when price approaches a resistance zone (within 3 pips) with 3 consecutive bullish candles, there is a 40% chance of triggering a breakout — a sharp 15–30 pip move over 3–5 candles followed by a retest of the broken level. ARIA must detect and announce this in real time.

---

## 7. PHASES — MAKE EACH ONE MEANINGFUL

In `phases.ts`, ensure every phase has ALL of the following defined and actually used by the engine:

- `id`, `title`, `subtitle` (one compelling sentence describing what the trader will become)
- `ariaIntroMessage` (ARIA's full briefing speech for this phase, 2–3 sentences, specific and motivating)
- `conceptToTeach` (the single concept being taught: e.g. "Higher High / Higher Low structure")
- `chartAnnotations` (what ARIA draws on the chart during the lesson: `swingPoints` | `trendLine` | `srZones` | `emaLines` | `patternHighlight`)
- `challenge` object with: `title`, `description`, `criteria` (machine-readable conditions the engine checks), `xpReward`, `failureMessage`, `successMessage`
- `unlocksFeatures` (array of features unlocked: e.g. `['ema9', 'ema21', 'rsi', 'srZones', 'volumeBar']`)
- `requiredXP` to advance

The 12 phases in order: Market Orientation → Candlestick Reading → Trend Recognition → Support & Resistance → Entry Timing → Risk Management → EMA Confluence → RSI Divergence → Breakout Trading → Range Trading → Trade Psychology → Pro Simulation.

Each phase's challenge must be a task the user actually executes on the live chart — not a quiz, not a button click. They must open a real trade that meets the criteria.

---

## 8. ANIMATIONS — IMPLEMENT ALL OF THESE

Use Framer Motion for component animations and CSS keyframes for continuous/canvas animations:

- **Panel entrance**: all panels slide in from their respective edges on mount (left panel from left, right panel from right, top bar from top). Duration 400ms, easeOut.
- **ARIA entrance**: slides up from bottom-right, scales from 0.8 to 1.0, with a brief glitch effect (2 rapid horizontal translate jitters of 3px before settling). On subsequent appearances: fade in only.
- **Phase unlock**: full-screen overlay, dark with teal particle effect (50 small dots animate outward from center), phase title types itself in, ARIA avatar appears center-screen large format with her briefing.
- **XP gain**: number next to XP bar briefly shows "+XP" floating upward and fading. Bar fill animates. On level up: bar flashes, screen vignette pulses teal once.
- **Trade close — win**: green particle burst from the trade panel, P&L number animates count-up to final value.
- **Trade close — loss**: screen briefly vignettes red (rgba(226,76,74,0.08) full-screen overlay, 500ms fade in/out). P&L animates count-down.
- **Achievement unlock**: card slides in top-right, stays 4 seconds, slides out. Achievement icon spins once on entry.
- **Candle close**: the completed candle briefly flashes (opacity 1 → 0.6 → 1 over 200ms) to signal the close.
- **Challenge complete**: the challenge card flips (CSS 3D rotateY 180deg) from the task face to a green success face showing ARIA's success message and XP earned.

---

## 9. SOUND DESIGN (OPTIONAL BUT IMPLEMENT IF POSSIBLE)

Use the Web Audio API (no external files needed — synthesize sounds):

- New candle close: very short soft tick (sine wave, 800hz, 30ms, low volume)
- Trade open: ascending two-tone chime (440hz then 550hz, 80ms each)
- Trade close win: short ascending three-note arpeggio (523, 659, 784hz)
- Trade close loss: short descending two-tone (440hz then 330hz)
- Phase unlock: longer ascending sweep (200ms, frequency ramp 400 → 1200hz) followed by a held tone (800hz, 300ms)
- Achievement unlock: bright chime (1047hz, 150ms)
- ARIA warning: low pulse (220hz, two pulses, 100ms each)

All sounds respect a global mute toggle in the settings. Default is ON.

---

## 10. PERFORMANCE & CORRECTNESS

- The chart canvas must never cause layout reflow. Use `position: absolute`, `will-change: transform` on canvas elements.
- The trading engine tick (price update interval) must use `setInterval` with a ref-stored ID that is properly cleared on unmount. Never create multiple intervals.
- All `useEffect` hooks must have correct dependency arrays. No stale closures. No infinite loops.
- The `useTradingEngine` hook must expose a stable API: all returned functions must be wrapped in `useCallback`, all computed values in `useMemo`.
- TypeScript must be strict. No `any` types anywhere. All phase definitions, trade objects, ARIA states, challenge criteria, and chart data must be fully typed with interfaces in a `/src/types/index.ts` file.
- The app must be fully responsive down to 1280px wide minimum. Below that, show a "Please use a wider screen" message. No mobile layout needed.
- Dev mode toggle (keyboard shortcut `Ctrl+Shift+D`) must unlock all phases instantly and fill XP for testing. This must work correctly and not break any state.

---

## DELIVERABLE

When you are done, the app must feel like this: A user opens it for the first time. ARIA appears and greets them with personality. The chart is alive and beautiful. They know exactly what to do because the challenge card tells them. They execute their first trade. ARIA watches and comments in real time. The trade closes. ARIA gives a specific verdict. XP flows in. Everything animates correctly. They feel like they are learning something real inside a premium product that respects their intelligence. That is the bar. Meet it.

---